# EmailJS — used by the Contact form to send messages directly to your inbox.
# Create a free account at https://www.emailjs.com, then fill these in and
# save this file as .env.local (which is gitignored).
VITE_EMAILJS_SERVICE_ID=
VITE_EMAILJS_TEMPLATE_ID=
VITE_EMAILJS_PUBLIC_KEY=
